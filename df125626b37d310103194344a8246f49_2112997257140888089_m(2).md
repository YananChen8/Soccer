# Soccer 项目进展总览

## 0. 全局远端路径与约束

| 项 | 服务器路径/约束 | 说明 |
|---|---|---|
| 远端代码根目录 | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate` | SoccerMaster / sn-gamestate 主工作树 |
| temporal/calib 统一结果 hub | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub` | temporal-HRNet、adapter-train、calibration 结果集中目录 |
| round2 HRNet heatmap cache | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/round2_temporal_calib/cache_hrnet` | 早期 HRNet heatmap 缓存 |
| 高精度标定论文复现根目录 | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/paper_repro/high_accuracy_calib_20260702` | high-accuracy calibration / synthetic 4DoF 主输出目录 |
| synthetic 4D 1M sparse DB | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/paper_repro/high_accuracy_calib_20260702/synthetic_4d_highres_sparse_1m_20260711` | synthetic 4D 高分辨率稀疏数据库 |
| synthetic 4D split 文件 | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/paper_repro/high_accuracy_calib_20260702/splits_synth4d_1m_seed20260712.json` | 1M synthetic split |
| 推荐远端 Python | `/remote-home/jiayuanrao/tools/anaconda/anaconda3/envs/wys_soccermaster/bin/python` | 新高精度标定任务优先使用 |
| 禁用 GPU | `202:6`, `200:1`, `200:5`, `200:6` | Soccer 远端任务硬约束 |

## 1. GameState 跟踪 / SAM3 / GS-HOTA

### 1.0 研究背景与研究目标

研究背景：SoccerNet GameState 任务不仅要求检测球员/球/裁判，还要求跨帧保持身份一致，并将图像检测结果通过场地标定映射到球场坐标。传统 SoccerMaster/ReID 或 YOLO+SAM2 方案在检测、分割、ID 关联之间存在误差传递，尤其在遮挡、远景小目标、镜头运动和多人密集区域中，检测质量和关联稳定性都会限制 GS-HOTA。

研究目标：验证 SAM3 这类视频分割/跟踪模型能否以更少的手工模块提升 GameState tracking。核心目标不是单帧分割好看，而是在官方 GS-HOTA 口径下提升 DetA、AssA 和 LocA，并找到可以扩展到多视频的稳定 prompt/刷新/去重评估流程。

### 1.1 已完成工作

- 对比过 SoccerMaster+ReID、YOLOv8+SAM2+ReID、SAM3 native、SAM3 periodic text-prompt 等跟踪方案。
- 在 `SNGS-021` 上，SAM3 periodic prompt 明显优于原始 SoccerMaster/SAM2 baseline。
- 修复过官方 eval 报 `KeyError: 'seq'` 的真实原因：同一帧内重复 `track_id`。修复方式是在 eval 前按 `(image_id, track_id)` 去重，保留最高 `bbox_conf`。

### 1.2 代表性结果

| 方法 | 数据/设置 | 结果 |
|---|---|---|
| SoccerMaster + ReID | `SNGS-021` | `GS-HOTA=52.546`, `GS-DetA=51.011`, `GS-AssA=54.134` |
| YOLOv8 + SAM2 + ReID | `SNGS-021` | `GS-HOTA=49.160`, `GS-DetA=45.486`, `GS-AssA=53.183` |
| SAM3 native | `SNGS-021` | `GS-HOTA=49.572`, `GS-DetA=37.370`, `GS-AssA=65.784` |
| SAM3 periodic50 | `SNGS-021` | `GS-HOTA≈64.073` |
| SAM3 periodic60/70/75 | `SNGS-021` | `GS-HOTA≈63.25-64.01` |

### 1.3 当前判断

SAM3 periodic text prompt 是 tracking 线最明显的正信号。但现有强结果集中在 `SNGS-021`，扩展到 valid10/更多视频时曾遇到远端磁盘满和评估流水线问题，需要先清理和复跑多视频评估。

## 2. NBJW Test-Time Adaptation

### 2.0 研究背景与研究目标

研究背景：NBJW-HRNet 是当前场地标定的重要 baseline，但它是离线训练好的单帧预测模型。真实比赛视频中，同一段视频的相机、场线、可见区域具有连续性，单帧模型没有显式利用这些测试时可见的时序和几何约束。直接重训大模型成本高、风险大，因此 test-time adaptation 是一种低成本修正路径。

研究目标：在不重新训练完整 NBJW 的前提下，利用测试视频自身的 flip consistency、几何伪标签、RANSAC 过滤和时间平滑约束，推理时小步更新或修正 keypoint/camera 输出。目标是在保持 CR 和 JaC 的同时降低 MRE/reproj，并改善 camera smoothness，形成一条可快速复现、可汇报的轻量方案。

### 2.1 已完成工作

围绕 NBJW-HRNet 输出做 test-time adaptation，主要比较：

- `flip_consistency`
- `pseudo_label`
- `pseudo_label_temporal`
- `pseudo_label_temporal_smoothgate`

核心思路是不重新大训练模型，而是在测试视频上用 flip consistency、RANSAC/几何伪标签、时间约束、smooth gate 做轻量自适应。

### 2.2 代表性结果

| 范围 | 方法 | 主要结果 |
|---|---|---|
| `SNGS-116..123`, stride 20 | `pseudo_label_temporal` | `Final Score=0.668039`, `JaC@5=0.723581`, `MRE=6.514` |
| Full49, stride 20 | baseline | `JaC@5=0.690669`, `MRE=10.264935`, `CR=0.943072`, `Final=0.652790`, `smooth_mean=640.202` |
| Full49, stride 20 | `pseudo_label_temporal_smoothgate` | `JaC@5=0.693674`, `MRE=9.504150`, `CR=0.945220`, `Final=0.656939`, `smooth_mean=518.155` |

Full49 指 `SNGS-116..150` 加 `SNGS-187..200`，共 49 个视频，stride 20，约 1862 帧。

### 2.3 推荐过的 TTA 配置

```bash
--methods pseudo_label_temporal
--steps 5
--lr 3e-5
--anchor-weight 0.05
--peak-weight 0.0
--pseudo-conf-threshold 0.05
--pseudo-ransac-px 12
--pseudo-sigma 2.0
--outlier-weight 0.001
--temporal-weight 0.2
--smooth-gate
--smooth-gate-ratio 0.8
--stride 20
```

### 2.4 当前判断

TTA 是目前最适合短期汇报的一条线：改动小、可解释、Full49 上有小幅但多指标一致的正信号。注意：图像 memmap 只缓存 raw image 时曾改变 preprocessing，不应直接作为官方 TTA 指标路径。

## 3. Temporal HRNet / Adapter-Train / Full Fine-Tune

### 3.0 研究背景与研究目标

研究背景：后置 temporal adapter 的失败说明，HRNet 输出之后再修正 heatmap/关键点，往往只能微调已有峰值，难以恢复漏检点或纠正一次性误检点。场地标定又是像素级精细任务，keypoint heatmap 的峰值位置、可见性和置信度直接影响后续 RANSAC/camera solve。因此瓶颈可能不在后处理，而在 HRNet 本身是否能感知时序上下文。

研究目标：把时序信息前移到 HRNet 上游或同级，使 keypoint HRNet 在预测 heatmap 时就能利用相邻帧信息。具体目标是在不破坏 NBJW 单帧定位能力的前提下，通过 `last_fusion` 或 `stage1_fusion` 等小型 temporal residual module，减少关键点抖动、短时漏检和低置信错误峰，并最终在 JaC/MRE/CR 与 camera smoothness 上超过同文件 baseline。

### 3.1 初始问题

早期只在 HRNet 输出 heatmap/关键点之后加 temporal adapter，试过 ST-GCN、Transformer、K=15/50、不同 LR、outlier-only、LoRA、peak/mass loss。核心教训是：如果 HRNet 原始 heatmap 不准，后置 adapter 很难凭空恢复漏检点；但盲目 dense offset 又会扰动本来正确的点。

### 3.2 结构探索

| 结构 | 插入位置 | 结论 |
|---|---|---|
| 后置 temporal adapter | HRNet heatmap 之后 | 泛化差，容易改坏好点 |
| `last_fusion` | HRNet encoder 后、head 前 | 最稳妥，保留单帧定位能力 |
| `stage1_fusion` / first | stem/stage1 后 | 更早期，理论上能修边缘/浅层特征，但更容易糊线 |
| LoRA + fusion | head/decoder 轻量可训参数 | 没有稳定大收益 |
| K5/K15 full finetune | 全参 HRNet + temporal adapter | 有轻微 accuracy 正信号，smooth 改善不稳定 |

### 3.3 Loss 迭代

| 阶段 | Loss/修复 | 结论 |
|---|---|---|
| fg loss / dense MSE | 早期 masked heatmap 训练 | 训练 loss 下降不等于 calibration 变好 |
| peak/missing hard loss | 试图对齐 peak 位置 | 容易导致概率铺开或不稳定 |
| mass/peak reward | 奖励 GT 半径内概率质量 | 不能保证 2px 尖峰更准 |
| official-aux | 复刻 NBJW masked L2 + peak/motion aux | 曾存在日志稀疏、scale 不清问题 |
| bgfix | 修复 background channel 57 重建 | background 应为 `clip(1 - sum(keypoint_heatmaps), 0, 1)`，不是 keypoint Gaussian |
| TensorBoard/logging | 科学计数法和 `x1e6` 字段 | 解决 `0.000024` 这类显示看不出变化的问题 |

### 3.4 代表性结果

| run | 初始化 | 结构 | test Final | test MRE | test smooth |
|---|---|---:|---:|---:|---:|
| baseline | NBJW | 无 temporal | `0.6528` | `10.2649` | `640.2` |
| `bgfix_k5_last_e3_resume5_noteacher_20260703` | 旧 temporal checkpoint | K5 last | `0.6573` | `9.8113` | `998.2` |
| `bgfix_k5_last_e6_noteacher_20260703` | NBJW 官方权重 | K5 last | `0.6556` | `10.0959` | `776.1` |
| `bgfix_k15_first_e6_noteacher_20260703` | NBJW 官方权重 | K15 stage1/first | `0.6533` | `9.1515` | `671.4` |

补充：训练集 stride 20 曾出现四组都提升 point/line/smooth 的结果，例如 `stage1_motion` train reproj 最好、`last_nomotion` train smooth 最好；但 test 上不稳定，不能只看 train。

### 3.5 加速与缓存

已明确当前瓶颈主要不是 GPU 算力，而是 CPU 数据流水线：

- JPG/JSON 每帧现读。
- `KeypointsDB(...).get_tensor_w_mask()` 现算 GT heatmap。
- 早期没有 PyTorch `Dataset/DataLoader`、`num_workers`、prefetch。
- 评估阶段还要 CPU 做 decode、RANSAC/camera solve、score_frame、smooth。

探索过的方向：

| 方向 | 结论 |
|---|---|
| window-level cache | K 变大时空间爆炸，不适合 |
| frame-level compact label cache | 更合理，GT/label 只缓存一次 |
| uint8 image memmap + compact labels | 小样本吞吐约 265 samples/s，估算全量约 115GB，低于 400GB |
| 只缓存 raw image memmap 用于 TTA | 对 true TTA 加速有限，还可能改变 preprocessing |
| 更值得缓存 | `kp_hm`, `line_hm`, decoded keypoints, solver inputs |

### 3.6 确认过的远端结果目录

| 主题 | 服务器路径 |
|---|---|
| temporal hub | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub` |
| bgfix full finetune | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_bgfix_20260702` |
| peakfix | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_peakfix_20260702` |
| lossfix | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_k3_lossfix_l2mask` |
| official-aux | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_k3_official_aux_20260701` |
| cached-speed | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_k3_cached_speed_20260701` |
| memmap cache | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_k3_memmap_cache_20260701` |

### 3.7 当前判断

Temporal HRNet 不是完全没信号，但目前“提升 accuracy”和“减少 smooth 抖动”没有同时稳定成立。下一步不建议盲目更长 epoch；更应该做 conservative finetune、冻结 backbone、推理阶段 gate/fallback、checkpoint/video 级错误分析，或先做 K15 last feature cache 解决训练速度。

## 4. 后处理、Kalman、Outlier、Primitive、BroadTrack-style

### 4.0 研究背景与研究目标

研究背景：场地标定 pipeline 的误差不仅来自神经网络 heatmap，也来自 keypoint 选择、RANSAC、camera optimization 和结构先验使用方式。很多错误是少量粗大 outlier 导致的 camera 解偏移，而不是所有点都需要被修正。因此在不重训 HRNet 的情况下，后处理和结构化约束仍然可能带来收益。

研究目标：验证哪些轻量后处理能在不破坏好点的情况下修正坏点。重点包括 keypoint Kalman、outlier-only two-RANSAC、primitive-level gate/refine、BroadTrack-style flow/radial/tripod 等。最终目标不是单纯降低 calibration-only 指标，而是找到能安全迁移到 GS-HOTA 的 per-frame correction/fallback 机制。

### 4.1 Round2/Round3 temporal calibration

| 方法 | 结果/结论 |
|---|---|
| ST-GCN/Transformer dense adapter | Full49 泛化差，point/line/reproj/smooth 多数变差 |
| Kalman keypoint | calibration-only 有提升；只能救有连续历史的点 |
| Kalman camera | 更像平滑器，可能严重伤 accuracy |
| Outlier-only two-RANSAC | 比全点盲修合理；只修 RANSAC outlier 有小正信号 |

已知统计：

- Full49 检测关键点约 24250 个。
- RANSAC outliers 约 2554 个，占约 10.53%。
- Kalman 可替换 outlier 约 397 个，占约 1.64%。

关键教训：大多数点不是 outlier，dense temporal offset 会扰动好点；更合理的是 outlier-gated correction/deletion。

### 4.2 GS-HOTA 注入结果

Outlier-only calibration 在 calibration-only 指标上有提升，但注入 frozen SAM3 tracking state 后 GS-HOTA 下降：

| 设置 | GS-HOTA | DetA | AssA | LocA |
|---|---:|---:|---:|---:|
| frozen SAM3 原始 | `63.317` | `54.834` | `73.113` | `93.308` |
| outlier-only calibration 替换 | `62.567` | `54.125` | `72.326` | `92.912` |

结论：calibration-only 变好不等于 GS-HOTA 变好。最终 tracking 侧必须做 per-frame fallback/gating。

### 4.3 Primitive-level refine

| 方向 | 结论 |
|---|---|
| box/goal 结构项 | 有一定价值 |
| field/VP | 容易有害 |
| soft weighting | 容易被粗大错误点带飞 |
| strict primitive-only refine | 不够稳 |

重要纠错：primitive refine 不是移动 keypoint，而是用加权非线性最小二乘 refine camera。旧视觉样例里 circle/arc 语义曾混淆，不能作为严格证据。

### 4.4 BroadTrack-style

| 机制 | 结论 |
|---|---|
| flow | 小幅改善 point/reprojection，但 line/smooth 可能变差 |
| radial_k1 | 强改善 line/reproj/smooth，但 point 下降 |
| tripod | soft camera-center gate 中性或有害，不值得优先扩展 |

BEV 可视化只说明 calibration 变化，不等于 tracking/GS-HOTA 重新评估。

### 4.5 确认过的远端目录

| 主题 | 服务器路径 |
|---|---|
| round2 HRNet heatmap cache | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/round2_temporal_calib/cache_hrnet` |
| temporal/calib 统一 hub | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub` |

## 5. 高精度场地标定论文复现 / Synthetic 4DoF Retrieval

### 5.0 研究背景与研究目标

研究背景：NBJW 依赖检测场地点/线后求解相机，而高精度标定论文类方法通常尝试利用 synthetic camera database、rendered field templates、feature retrieval 或参数回归提高全局搜索和精细标定能力。这类方法的潜在优势是可以引入大量合成相机姿态作为先验，但难点是 synthetic-rendered 特征与真实比赛帧之间存在 domain gap。

研究目标：以最小可运行方式复现高精度标定思想，确认 synthetic DB / retrieval / projection-aware training 是否能超过 NBJW baseline。核心目标包括：建立标准 full-DB 评估口径，分清 oracle 上限与 direct predictor 能力，判断瓶颈到底是 DB 覆盖不足、特征空间不对齐，还是模型训练目标不合理。

### 5.1 已完成工作

目标是复现或迁移论文 `A High Accuracy Camera Calibration Method for Sport Videos` 的核心思想。

阶段性路线：

1. 最小 proxy：用 NBJW 输出和 synthetic camera DB 做匹配。
2. 发现 proxy 问题：`line heatmap -> rendered camera-polylines` 是 feature-domain mismatch，不是严格论文复现。
3. 转向 synthetic 4DoF / HRNet retrieval / projection-aware loss。
4. 建立 full DB 标准指标：`point_acc`, `line_acc`, `reproj_mean/MRE`, `JaC@5/10/15/20`。

### 5.2 代表性结果

| 训练规模/方法 | 结论 |
|---|---|
| corrected 100k `feat mixedproj+rankproj` | 当前较好 direct 结果，`JaC@20=0.8592309581`, `MRE=9.5924757250` |
| 500k oracle | oracle 上限提高到约 `JaC@20=0.9709` |
| 500k direct | `JaC@20≈0.8283`，低于 100k best |
| 900k oracle | oracle 上限提高到约 `JaC@20=0.9785` |
| 900k direct | `JaC@20≈0.7140`，明显退化 |

核心判断：DB 密度和 oracle 上限很强，但 learned direct predictor / retrieval embedding 的泛化是瓶颈，不是简单扩大 DB 就能解决。

### 5.3 确认过的远端目录

| 主题 | 服务器路径 |
|---|---|
| 高精度标定根目录 | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/paper_repro/high_accuracy_calib_20260702` |
| synthetic 4D 1M sparse DB | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/paper_repro/high_accuracy_calib_20260702/synthetic_4d_highres_sparse_1m_20260711` |
| synthetic 4D split 文件 | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/paper_repro/high_accuracy_calib_20260702/splits_synth4d_1m_seed20260712.json` |

## 6. 数据、评估与报告口径

### 6.1 标准评估指标

正式 calibration 比较应使用：

| 指标 | 含义 |
|---|---|
| `point_acc` | 点检测/标定相关点精度 |
| `line_acc` | 线检测/标定相关线精度 |
| `reproj_mean` / `MRE` | 平均重投影误差，单位 px |
| `JaC@5/10/15/20` | 不同像素阈值下正确重投影比例 |
| `CR` | 有效输出相机参数比例 |
| `Final Score` | 通常为 `CR * JaC@5` |
| `camera_smooth_l2_mean/p95` | 相机参数连续性诊断，不是官方 ranking 主指标 |

## 7. 当前总体结论

### 7.1 已经比较可靠的结论

1. **TTA 是短期最可汇报的正结果**：`pseudo_label_temporal_smoothgate` 在 Full49 上同时改善 Final/MRE/smooth，幅度不大但方向一致。
2. **后置 temporal adapter 不是主线**：它容易修坏好点，尤其 dense offset 不适合直接全点修正。
3. **Temporal HRNet 有信号但不稳定**：full fine-tune 可以略微改善 JaC/MRE，但 smoothness 没有稳定变好。
4. **calibration-only 不能直接等价于 GS-HOTA**：outlier-only calibration 改善后注入 frozen SAM3 反而降低 GS-HOTA。
5. **高精度标定 synthetic DB 的 oracle 上限强**：但 direct predictor 的泛化是当前瓶颈。

### 7.2 下一步建议

| 优先级 | 建议 |
|---|---|
| P0 | 固化 TTA 最佳配置，形成一版可复现 Full49 报告和可视化 |
| P1 | Temporal HRNet 不再盲目加 epoch，先做 checkpoint/video 级错误分析和 conservative finetune |
| P1 | 对 temporal 输出加 inference gate/fallback，只在高置信可改善帧替换 baseline |
| P2 | 高精度标定线继续研究 retrieval embedding 和 projection-aware training，而不是单纯扩大 DB |
| P2 | SAM3 periodic 扩展到更多视频前，先解决远端磁盘和 eval 去重流水线 |

## 8. 确认服务器路径索引

| 主题 | 服务器路径 |
|---|---|
| 远端代码根目录 | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate` |
| temporal/calib hub | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub` |
| round2 HRNet heatmap cache | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/round2_temporal_calib/cache_hrnet` |
| bgfix full finetune | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_bgfix_20260702` |
| peakfix | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_peakfix_20260702` |
| lossfix | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_k3_lossfix_l2mask` |
| official-aux | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_k3_official_aux_20260701` |
| cached-speed | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_k3_cached_speed_20260701` |
| memmap cache | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/gsr/temporal_hrnet/temporal_calib_results_hub/full_finetune_temporal_nbjw_k3_memmap_cache_20260701` |
| 高精度标定根目录 | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/paper_repro/high_accuracy_calib_20260702` |
| synthetic 4D 1M sparse DB | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/paper_repro/high_accuracy_calib_20260702/synthetic_4d_highres_sparse_1m_20260711` |
| synthetic 4D split 文件 | `/remote-home/jiayuanrao/yishan/SoccerMaster/codes/sn-gamestate/outputs/paper_repro/high_accuracy_calib_20260702/splits_synth4d_1m_seed20260712.json` |
