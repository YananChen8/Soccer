# SoccerMaster / SAM3 GSR 本周进展报告

## 1. 本周目标

本周主要围绕以下问题展开：

1. 将混合多项任务的 GS-HOTA 拆分为具体、独立的评价指标；
2. 建立 SoccerMaster baseline、SAM2 refined 和 SAM3 的统一评估口径；
3. 测试 SAM3 periodic prompting 是否能够改善视频跟踪；
4. 尝试使用多 GPU 完成整段视频推理，避免 chunk 拼接造成的轨迹断裂。

---

## 2. Atomic GSR 评估体系

新增独立评估工具：

```text
tools/eval_atomic_gsr.py
```

该工具不再只输出综合 GS-HOTA，而是针对不同任务分别计算指标：

| 任务目标  | 主要指标                        | 含义                                   |
| ----- | --------------------------- | ------------------------------------ |
| 人物检测  | Image DetA                  | 图像空间下的有效检测准确性                        |
| 轨迹重建  | Image AssA                  | 预测轨迹与 GT 轨迹的长期关联质量                   |
| 检测框定位 | Image LocA/IoU              | 成功匹配目标的平均检测框 IoU                     |
| 球场位置  | Pitch LocA                  | 成功匹配目标的球场坐标准确性                       |
| 角色识别  | Role Macro-F1               | player、goalkeeper、referee 等角色的宏平均 F1 |
| 队伍识别  | Team Track Accuracy         | 轨迹级队伍识别准确率                           |
| 球号识别  | Jersey Track Exact Accuracy | 轨迹级球衣号码完全匹配准确率                       |

其中，Image HOTA 仍作为检测与轨迹关联的辅助综合指标：

$$
\text{Image HOTA} \approx \sqrt{\text{Image DetA}\times\text{Image AssA}}
$$

但后续分析主要使用各项 atomic metric，不再只依赖一个综合分数。

评估器还增加了以下处理：

* 自动修复同一帧、同一 `track_id` 的重复预测；
* 将属性匹配与图像检测匹配分离；
* 检测无效的 `bbox_pitch` 占位坐标；
* 对未实际运行的 role、team、jersey 模块进行识别，避免将占位结果误认为真实性能。

---

## 3. 实验设置

本周在 `SNGS-021` 的750帧视频上比较了四种方法。

### 3.1 SoccerMaster baseline

使用已经完成 Step3 的 SoccerMaster + ReID state：

```text
outputs/gsr/01_gs_hota_valid10/step3_pklz/
final_step3_soccermaster_reid_valid10_021/
states/sn-gamestate.pklz
```

### 3.2 SAM2 refined

使用完成 SAM2 refinement 和后续 ReID 处理的 state：

```text
outputs/gsr/01_gs_hota_valid10/step3_pklz/
final_step3_sam2_reid_valid10_021/
states/sn-gamestate.pklz
```

### 3.3 SAM3 native chunk100

将视频每100帧划分为一个独立 chunk，在每个 chunk 内运行 SAM3，最后通过 stitch 连接不同 chunk 的轨迹。

### 3.4 SAM3 periodic50 chunk100

同样使用100帧 chunk，但在每个 chunk 内每50帧重新进行一次 periodic prompt/reconditioning。

因此，SAM3 native 与 periodic50 的唯一区别是是否增加 periodic prompting；两者都使用 chunk100 和跨 chunk stitch。

---

## 4. 图像检测与轨迹结果

| 方法                       | Image DetA | Image AssA | Image HOTA | Image LocA/IoU | 匹配轨迹数 |   预测数 |
| ------------------------ | ---------: | ---------: | ---------: | -------------: | ----: | ----: |
| SAM2 refined             |     70.198 |     45.005 |     56.129 |         85.207 |    22 | 13026 |
| SoccerMaster baseline    | **75.211** |     51.108 |     61.969 |     **85.579** |    23 | 11815 |
| SAM3 native chunk100     |     72.047 |     56.772 |     63.917 |         83.746 |    23 | 12172 |
| SAM3 periodic50 chunk100 |     71.998 | **56.998** | **64.020** |         83.679 |    23 | 12164 |

GT detection 总数为：

```text
11926
```

### 4.1 SAM3 相对 baseline

以 SAM3 periodic50 为例：

| 指标             | Baseline | SAM3 periodic50 |         变化 |
| -------------- | -------: | --------------: | ---------: |
| Image DetA     |   75.211 |          71.998 |     -3.213 |
| Image AssA     |   51.108 |          56.998 | **+5.890** |
| Image HOTA     |   61.969 |          64.020 | **+2.051** |
| Image LocA/IoU |   85.579 |          83.679 |     -1.900 |

结果表明：

* SAM3 的轨迹关联质量明显更高；
* SAM3 在 AssA 上提升约5.9分；
* 尽管 SAM3 的 DetA 和框 IoU 较低，其辅助综合 Image HOTA 仍比 baseline 高约2.05分。

SAM3 当前的主要优势不是检测框，而是**跨帧轨迹关联能力**。

### 4.2 Periodic prompting 消融

| 指标             | Native | Periodic50 |     变化 |
| -------------- | -----: | ---------: | -----: |
| Image DetA     | 72.047 |     71.998 | -0.049 |
| Image AssA     | 56.772 |     56.998 | +0.226 |
| Image HOTA     | 63.917 |     64.020 | +0.103 |
| Image LocA/IoU | 83.746 |     83.679 | -0.067 |

所有变化均小于0.3分，说明 periodic50 没有带来实质提升。

当前可以得出：

因此，下一阶段不再优先进行 periodic25、periodic50、periodic100 等间隔扫描。


## 5. 球场位置与属性结果

| 方法                    | Pitch LocA | Role Macro-F1 | Team Track Accuracy | Jersey Track Accuracy |
| --------------------- | ---------: | ------------: | ------------------: | --------------------: |
| SoccerMaster baseline | **93.852** |        75.000 |         **100.000** |                77.778 |
| SAM2 refined          |      无有效结果 |        75.000 |              95.000 |                77.778 |
| SAM3 native           |       尚未运行 |          尚未运行 |                尚未运行 |                  尚未运行 |
| SAM3 periodic50       |       尚未运行 |          尚未运行 |                尚未运行 |                  尚未运行 |

SAM2 state 中的 `bbox_pitch` 被评估器识别为图像坐标占位值，因此不能用于 Pitch LocA。

当前 SAM3 state 只包含人物检测与轨迹信息：

* `role` 被默认写为 `player`；
* 没有真实 team 预测；
* 没有真实 jersey number 预测；
* `bbox_pitch` 不是 calibration 后的球场坐标。

因此，SAM3 输出中的 Role Macro-F1 23.256、Team 0 和 Jersey 0 不代表真实属性识别能力，正式对比中应记为 `N/A`。

---

## 6. Full-video 多 GPU 推理

为了避免 chunk100 带来的跨 chunk stitch，尝试使用4张 GPU 对完整750帧视频运行同一个 SAM3 session，并启用：

```text
offload_video_to_cpu=True
offload_state_to_cpu=True
```

推理运行到第305帧后停止，随后出现 NCCL timeout。

### 6.1 已定位的根因

所有 worker 初始化时均使用默认配置：

```text
max_num_objects=10000
recondition_every_nth_frame=16
```

但 worker 启动后，主进程单独将 rank 0 修改为：

```text
max_num_objects=48
recondition_every_nth_frame=0
```

实际配置变成：

| Rank     | max_num_objects | recondition interval |
| -------- | --------------: | -------------------: |
| Rank 0   |              48 |                    0 |
| Rank 1–3 |           10000 |                   16 |

不同 rank 因此执行了不同的控制流和 collective operation：

```text
Rank 0：ALLREDUCE
Rank 1–3：ALLGATHER
```

最终各 rank 相互等待，并在1800秒后触发 NCCL timeout。

这说明当前问题不是 SAM3 不支持多 GPU，也不是简单增加 timeout 可以解决，而是运行参数没有在 worker 创建前同步到所有 rank。

## 队伍识别改进方案

队伍识别包含两个相互独立的问题：

1. **队伍聚类**：将普通球员正确划分为两个队伍；
2. **左右映射**：将两个无语义的聚类结果映射为 `left` 和 `right`。

因此，应分别分析个别球员错分和整队标签翻转，而不能仅使用最终准确率判断队伍识别能力。

### 1. 当前方法

当前队伍识别首先对同一轨迹内的 ReID embedding 求平均，得到 track-level embedding；随后使用 KMeans 将 `role=player` 的轨迹划分为两个 cluster：

```text
Track-level ReID embedding
→ KMeans
→ cluster 0 / cluster 1
```

KMeans 输出的 `cluster 0` 和 `cluster 1` 不具有固定语义，因此还需要根据球场位置将其转换为 `left` 和 `right`。

当前 `team_side` 模块的处理方式为：

1. 收集两个 cluster 中所有 detection 的 `bbox_pitch.x`；
2. 分别计算两个 cluster 的平均球场横坐标；
3. 平均位置较小的 cluster 标记为 `left`；
4. 平均位置较大的 cluster 标记为 `right`；
5. goalkeeper 根据自身球场横坐标的正负单独判断队伍。

当前方法可能产生两类错误：

| 错误类型 | 表现 | 主要原因 |
|---|---|---|
| 个别球员错分 | 同一队中少数球员被分到另一队 | ReID 特征不能充分表达球衣颜色，或者轨迹图像包含草坪、遮挡和背景 |
| 整队标签翻转 | 两队内部划分正确，但 `left/right` 完全对调 | KMeans 标签无固定语义，左右映射规则判断错误 |

---

### 2. 队伍聚类改进

队伍聚类阶段只负责回答：

> 球员是否被正确划分为两个队伍。

该阶段暂时不考虑哪个 cluster 是 `left`，哪个 cluster 是 `right`。

计划采用以下无训练方法：

```text
SAM3 mask
→ 上衣与短裤分区
→ 颜色特征提取
→ 与现有 ReID embedding 融合
→ 轨迹级特征聚合
→ KMeans 两类聚类
```

具体包括：

1. 使用 SAM3 mask 去除草坪、观众和相邻球员等背景；
2. 将球员区域划分为上衣区域和短裤区域；
3. 分别提取 HSV/Lab 颜色直方图；
4. 保留当前已有的 ReID embedding；
5. 将颜色特征和 ReID 特征分别归一化后进行融合；
6. 在每条轨迹内部聚合多帧特征；
7. 仅对 `role=player` 的普通球员进行两类聚类；
8. referee 和 other 不参与聚类，goalkeeper 单独处理。

融合特征可以表示为：

$$
F_{\mathrm{team}}
=
\lambda F_{\mathrm{color}}
+
(1-\lambda)F_{\mathrm{ReID}}
$$

其中，$F_{\mathrm{color}}$ 表示球衣与短裤颜色特征，$F_{\mathrm{ReID}}$ 表示当前预训练 ReID 模型输出的外观特征。

实验中可以比较：

```text
λ = 0.00：仅使用 ReID
λ = 0.50：颜色与 ReID 等权
λ = 0.75：以颜色特征为主
λ = 1.00：仅使用颜色特征
```
---

### 3. Left/Right 映射改进

左右映射阶段负责回答：

> 已经得到的两个队伍 cluster，哪个对应 `left`，哪个对应 `right`。

当前方法直接对所有 detection 的球场横坐标求平均。这样会导致长轨迹拥有更大的权重，少数出现帧数较多的球员可能主导整个映射结果。

计划将其修改为“轨迹等权 + 时间投票”。

#### 4.1 轨迹等权统计

首先计算每条轨迹的球场横坐标中位数：

$$
x_p
=
\operatorname{median}_{t}
\left(
x_{p,t}
\right)
$$

然后计算每个 cluster 内所有球员轨迹位置的中位数：

$$
X_c
=
\operatorname{median}_{p\in c}
\left(
x_p
\right)
$$

其中：

- $x_p$ 表示轨迹 $p$ 的代表位置；
- $X_c$ 表示 cluster $c$ 的代表位置。

最终将 $X_c$ 较小的 cluster 映射为 `left`，将 $X_c$ 较大的 cluster 映射为 `right`。

该方法使每个球员轨迹具有相同权重，避免长轨迹对结果产生过度影响。

#### 4.2 逐帧位置投票

对于同时观测到两个 cluster 球员的帧，计算：

$$
d_t
=
\operatorname{median}
\left(
x_{\mathrm{cluster\,1},t}
\right)
-
\operatorname{median}
\left(
x_{\mathrm{cluster\,0},t}
\right)
$$

判断规则为：

- 当 $d_t>0$ 时，cluster 0 在该帧更靠左；
- 当 $d_t<0$ 时，cluster 1 在该帧更靠左。

最后对所有有效帧进行投票，两个 cluster 位置差越明显，该帧的投票权重越高。根据多数帧的判断结果确定最终的 `left/right` 映射。

正式输出仍使用：

```text
team = left / right
```

---

## 8. 下一阶段计划

### P0：完成无 chunk 的多 GPU 推理

* 将 `max_num_objects` 和 `recondition_every_nth_frame` 在 predictor 构造阶段同步到所有 rank；
* 先完成2 GPU、200帧 smoke test；
* 再运行4 GPU、750帧完整视频；
* 比较无 stitch 与 chunk100 的 Image AssA。

验证目标：

```text
Full-video AssA > 56.998
```

### P1：提升 SAM3 检测能力

当前 SAM3 的 AssA 已超过 baseline，但 DetA 仍低约3.2分。下一步优先对已有 SAM3 state 进行置信度阈值扫描，减少低质量预测，不重新运行 SAM3 推理。

验证目标：

```text
提高 Image DetA，同时保持 Image AssA 不明显下降
```

### P1：补全 SAM3 Game State

在最佳 SAM3 tracking state 上运行：

* camera calibration；
* pitch-space mapping；
* role recognition；
* team assignment；
* jersey number recognition。

最终得到可计算以下指标的完整 state：

```text
Pitch LocA
Role Macro-F1
Team Track Accuracy
Jersey Track Exact Accuracy
```

### P2：扩展到 valid10

在 `SNGS-021` 完成方法选择后，将 baseline、SAM2 和最佳 SAM3 配置扩展到相同的 valid10 视频集合，验证结果是否具有跨视频稳定性。

### P3：测试队伍方案